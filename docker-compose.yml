version: '3.8'

services:
  backend:
    build: ./prediction_model
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./webUI
    ports:
      - "3000:3000"
    depends_on:
      - backend
